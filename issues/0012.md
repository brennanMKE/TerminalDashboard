# 0012 — Implement TabBar for auto mode

| | |
|---|---|
| **Status** | resolved |
| **Module** | Views |
| **Platform** | macOS |
| **First seen** | 2026-05-22 |
| **Closed** | 2026-05-22 |
| **Commit** | 48799d0 |

## Description

The auto mode shell requires a persistent tab bar at the top showing all three views with activity badges and an AUTO mode indicator.

## Expected behavior

- One tab per view: `[G] Git`, `[C] Crashes`, `[L] Logs`.
- Active tab is bold/highlighted.
- Inactive tabs show a badge for unseen significant events:
  - `●` — one new event since last viewed.
  - `●●` — two or more events.
  - Badge clears when that view becomes active.
- `AUTO` indicator on the right: bright when Auto Mode is active, dim when suspended.
- Transient countdown banner replaces normal tab bar content during an auto switch: `"Auto → Logs: error detected — returning in 8s"`. Returns to normal after the 10s window elapses or is cancelled.
- Tab bar is a single fixed-height row at the top of the terminal.

## Actual behavior

No tab bar exists.

## Notes

The tab bar should be driven by observable state from `AutoCoordinator` — badge counts, active view, auto mode flag, and the countdown timer value. The countdown display counts down in real time (updating each second).

## Root cause

No `TabBar` component existed in the Views module. The auto mode shell had no persistent header row to show tab state, badges, or the AUTO indicator.

## Fix

Created `Sources/TerminalDashboard/Views/TabBar.swift` with a single-row `HStack` layout:

- Three tab items built with `tabItem(source:shortcut:label:)` — each wraps a `[X] Label` `Text` (bold when active) plus a badge `Text` (yellow `●` / `●●` when count > 0 and tab is inactive).
- A `Spacer()` to push the right slot to the trailing edge.
- A `rightSlot` `@ViewBuilder` that renders the `bannerMessage` in yellow when non-nil, or the `AUTO` indicator in bright green (auto active) / gray (suspended).
- Only confirmed SwiftTUI APIs are used: `HStack`, `Text`, `Spacer`, `.bold()`, `.foregroundColor()`, `.frame(maxWidth:alignment:)`.

## Verification

`./build.sh build` completes with `Build complete!` and zero errors or warnings.

## Files changed

- `Sources/TerminalDashboard/Views/TabBar.swift` (new, 86 lines)
